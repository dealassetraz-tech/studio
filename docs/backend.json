{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account within the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "fullName": {
          "type": "string",
          "description": "User's full name."
        },
        "role": {
          "type": "string",
          "enum": ["seller", "buyer", "broker"],
          "description": "The user's role in the application."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "fullName",
        "role",
        "createdAt",
        "updatedAt"
      ]
    },
    "Property": {
      "title": "Property",
      "description": "Represents a real estate property listed in the application.",
      "type": "object",
      "properties": {
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who owns the property."
        },
        "address": {
          "type": "string",
          "description": "The full address of the property."
        },
        "price": {
          "type": "number",
          "description": "The listing price of the property."
        },
        "status": {
          "type": "string",
          "enum": ["Listed", "Under Contract", "Sold"],
          "description": "The current status of the property."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the property was listed."
        }
      },
      "required": ["ownerId", "address", "price", "status", "createdAt"]
    },
    "Deal": {
      "title": "Deal",
      "description": "Represents a transaction or deal for a property.",
      "type": "object",
      "properties": {
        "propertyId": {
          "type": "string",
          "description": "The ID of the property this deal is for."
        },
        "sellerId": {
          "type": "string",
          "description": "The UID of the seller."
        },
        "buyerId": {
          "type": "string",
          "description": "The UID of the buyer."
        },
        "status": {
          "type": "string",
          "enum": ["Active", "Closed", "Cancelled"],
          "description": "The current status of the deal."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the deal was initiated."
        }
      },
      "required": ["propertyId", "sellerId", "buyerId", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "Stores user profiles, with path-based ownership for security.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, corresponding to their Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/properties/{propertyId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/Property"
          },
          "description": "Contains all property listings.",
          "params": [
            {
              "name": "propertyId",
              "description": "The unique identifier for the property."
            }
          ]
        }
      },
      {
        "path": "/deals/{dealId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/Deal"
          },
          "description": "Contains all deals related to properties.",
          "params": [
            {
              "name": "dealId",
              "description": "The unique identifier for the deal."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "A collection of admin user IDs, used for role-based access control. Existence in this collection grants admin privileges.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      }
    ]
  }
}
